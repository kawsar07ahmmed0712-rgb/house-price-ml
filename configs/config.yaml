project:
  seed: 42

paths:
  raw_dir: data/raw
  processed_dir: data/processed
  artifacts_dir: artifacts

data:
  dataset_name: california_housing

training:
  test_size: 0.2


data:
  dataset_name: california_housing
  raw_filename: california_housing_raw.csv


data:
  dataset_name: california_housing
  raw_filename: california_housing_raw.csv
  target_column: MedHouseVal

training:
  test_size: 0.2
  random_state: 42

processed:
  train_filename: train.csv
  test_filename: test.csv



training:
  test_size: 0.2
  random_state: 42
  model_type: ridge
  ridge_alpha: 1.0

artifacts:
  model_filename: model_pipeline.joblib
  metrics_filename: metrics.json


artifacts:
  model_filename: model_pipeline.joblib
  metrics_filename: metrics.json
  sample_request_filename: sample_request.json
  sample_response_filename: sample_response.json
